# Pilot infrastructure review

Prepared 2026-09-05; updated 2026-09-08. Status: strict local synthesis and the exact main-branch OIDC review workflow passed; **no usable pilot is deployed**. The first authorized create failed on the account's Lambda concurrency quota and was recovered. A fresh exact-main change set is required for the corrected retry. The existing `StayDemoStack` remains outside the pilot change set.

The candidate adds one stack with its own encrypted product table/key, eleven Lambda functions including CDK helpers, eleven log groups, six SQS queues, two SNS topics, six actionable alarms, a separate required-MFA Cognito pool, an SES feedback configuration and the pilot API/domain. The existing Route53 zone and verified SES identity are referenced. Deployment reuses the existing repository-scoped main-branch OIDC role; the pilot stack creates no second deployment role or OIDC provider. The legacy `SesRecipientEmail` parameter and Lambda environment variable are absent from the pilot template; recipients come only from private household contact records. The diff creates pilot resources; the demo is not selected for update. Alias-name tests explicitly protect the judge apex.

[Workflow 34164714532](https://github.com/AmirmLotfy/stay/actions/runs/34164714532) reviewed exact source `1adafd50519dba6d595832d3b136dfd7bb49d086` and template SHA-256 `8168d9d19b49d5bd01bc425fae830b666294c13437bed52717c8f080efe36354`. Its non-executed change set `stay-pilot-review-1adafd50519dba6d595832d3b136dfd7bb49d086` is `CREATE_COMPLETE` and `AVAILABLE`, with 150 additions and no modifications or removals. The synthesized template has 151 resources because `BedrockInvokePolicy` is conditional and is absent from the provider change set while `BedrockModelId` is empty. The exact change set contains no CloudFront distribution, Bedrock resource, Route53 hosted zone or GitHub OIDC provider. It adds only the two pilot DNS records under the imported parent zone. All deployment and post-deployment verification steps were skipped.

The local AWS CLI session was refreshed on 2026-09-08 and identifies as account root. It was used only for read-only inspection and must not be used for pilot mutations. Pilot deployment uses the existing GitHub OIDC role. Fresh IAM inspection confirms a one-hour session and trust limited to `token.actions.githubusercontent.com`, STS audience, and the exact immutable subject `repo:AmirmLotfy@178108135/stay@1354119197:ref:refs/heads/main`. The role has no attached managed policies; its single inline policy covers CloudFormation change-set/artifact operations and assumption of the four account/region CDK bootstrap roles. Operator actions require the separate least-privilege policy emitted by the pilot stack. The first reviewed change set was executed and then removed with the failed stack; a new one must be prepared from the corrected main revision. Current SES state is sandbox mode with sending enabled, 200 messages/24h, one message/second, zero sends in the last 24 hours and zero account-level suppressed destinations. The `saystay.site` identity is verified and DKIM is successful. This is enough only for individually verified pilot recipients; inbox receipt and feedback tests remain unperformed. Bedrock and CloudFront remain outside this candidate's requirements.

## Advisory monthly estimate

US East (N. Virginia), rechecked 2026-09-08 against current AWS pricing pages and the exact synthesized resource inventory. Five households, at most 45 active identities, 100,000 HTTP requests, 100,000 WebSocket messages and 500,000 connection minutes/month; 200,000 Lambda invocations averaging 512 MB/0.5 seconds, plus 1,000 paced email sends; 2 million DynamoDB read units and 200,000 write units including transactions, one GB stored and backed up; one GB log ingestion. These are workload assumptions, not measured usage. Free tiers/credits are excluded from the calculations.

| Component                                       | Calculation or allowance                                                                                                           | Monthly USD |
| ----------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- | ----------: |
| KMS key and requests                            | One key $1; allow 100,000 requests at $0.03/10,000                                                                                 |       $1.30 |
| Secret                                          | One at $0.40 plus request allowance                                                                                                |       $0.45 |
| CloudWatch                                      | Six standard alarms $0.60; one pilot custom metric $0.30; 1 GB logs $0.50 plus storage/query allowance                             |       $1.60 |
| API Gateway                                     | 0.1M HTTP × $1/M; 0.1M WS messages × $1/M; 0.5M connection minutes × $0.25/M                                                       |       $0.33 |
| Lambda                                          | 50,000 GB-seconds × conservative x86 rate $0.0000166667, $0.04 requests, plus pacing/worker allowance; deployment uses cheaper ARM |       $1.10 |
| DynamoDB                                        | 2M reads × $0.125/M + 0.2M writes × $0.625/M + 1 GB storage $0.25 + PITR $0.20                                                     |       $0.83 |
| Cognito                                         | 45 Plus MAUs × $0.020 (configured threat protection tier)                                                                          |       $0.90 |
| SES                                             | 1,000 messages × $0.10/1,000 plus small transfer allowance                                                                         |       $0.11 |
| **Identified subtotal**                         | Sum of the rows above                                                                                                              |   **$6.62** |
| Queues, SNS, schedules, S3, traces and transfer | Provisional combined allowance; validate observed usage                                                                            |        $2–5 |
| **Incremental pilot planning range**            | Rounded with contingency                                                                                                           |   **$9–13** |

The pilot uses one total-event custom metric in `STAY/Pilot`; event/aggregate detail remains in logs. Detailed API route metrics are disabled for this stage; default service metrics and the six alarms remain. The two pilot DNS records are A/AAAA aliases to API Gateway in the existing hosted zone: there is no incremental hosted-zone fee, and Route53 does not charge queries for aliases to API Gateway. Current demo `STAY/Demo` lists 30 metric series. At $0.30/series for continuous full-month reporting those alone would be $9; actual billing is prorated by reporting hours and this inventory is not a bill. Existing demo route metrics and usage add further cost. A conservative combined allowance is **$20–30/month**, so the $25 target is still unproven. A live read-only Cost Explorer query on 2026-09-08 returned effectively $0 estimated unblended account cost for September 1–9, and the existing `STAY demo monthly alert` budget reported $0 calculated actual spend against its $25 limit. Free allowances, credits and reporting lag can make both values incomplete; neither establishes a forecast.

The existing $25 STAY budget and the pilot's $15 investigation/$20 enrollment-pause thresholds are informational and do not stop usage. Investigate at $15 forecast, pause enrollment at $20 forecast until reviewed, and obtain the operator's decision before expanding usage. Existing coordination must not be silently disabled by a budget alert. Rehearsal backups/restored tables, log growth, continuous client polling and unrelated account use can exceed these assumptions.

Rate sources rechecked 2026-09-08: [CloudWatch](https://aws.amazon.com/cloudwatch/pricing/), [KMS](https://aws.amazon.com/kms/pricing/), [Secrets Manager](https://aws.amazon.com/secrets-manager/pricing/), [API Gateway](https://aws.amazon.com/api-gateway/pricing/), [Lambda](https://aws.amazon.com/lambda/pricing/), [DynamoDB](https://aws.amazon.com/dynamodb/pricing/), [Cognito](https://aws.amazon.com/cognito/pricing/), [SES](https://aws.amazon.com/ses/pricing/), [SQS](https://aws.amazon.com/sqs/pricing/), [EventBridge](https://aws.amazon.com/eventbridge/pricing/) and [Route53](https://aws.amazon.com/route53/pricing/). The table is a reproducible planning calculation, not a saved AWS Calculator quote. The grouped $2–5 allowance and combined demo range need operator review before deployment.

**Deployment cost gate:** proceed only after the operator explicitly accepts that the pilot has no automatic spending stop and that combined demo-plus-pilot usage is modeled at $20–30/month. The $15 investigation and $20 enrollment-pause thresholds reduce exposure but do not guarantee the $25 target.

## Outstanding release evidence

Owner review of the final candidate and combined cost forecast; separate deployment authorization; confirmed SNS owner subscription and alert receipt; verified inbox/SES feedback tests; authenticated pilot tests; restore/rollback rehearsal; real-device accessibility and observation periods. See `pilot-runbook.md`.
