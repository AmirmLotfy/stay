# Product and developer friction log

Severity scale: **High** blocks a required track, deployment, safety, or delivery capability; **Medium** blocks a verification or requires a material workaround; **Low** creates avoidable maintenance or documentation friction without blocking the release.

| Date       | Surface                    | Task attempted and steps taken                                                                                                                                                                    | Expected vs. actual                                                                                                                                                                                            | Severity | Workaround used                                                                                                                                                                                               | Actionable suggestion                                                                                                       |
| ---------- | -------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------- |
| 2026-09-02 | Alexa+ access              | Checked the Alexa+ developer entrypoint and tried to establish a general add-on/device test path.                                                                                                 | Expected a public eligibility or request status; actual MCP Toolkit/add-on access was limited to selected partners.                                                                                            | High     | Built one standards-based MCP server plus a polished web simulator and kept real Alexa deployment as separate unverified evidence.                                                                            | Publish an account-level eligibility and request-status endpoint with the testing surfaces available to that account.       |
| 2026-09-02 | Alexa+ docs / MCP          | Compared the hackathon protocol requirement with current Alexa client examples and exercised both initialization eras.                                                                            | Expected one documented `2025-11-25` path; actual examples referenced a legacy 2025 initialization while the event requires a newer spec.                                                                      | High     | Registered tools once and served modern Streamable HTTP plus the stateless legacy initialization path; added negotiation tests.                                                                               | Publish a versioned client/protocol compatibility matrix with tested initialize, response-mode, and transport examples.     |
| 2026-09-02 | AWS authentication         | Completed approved remote login, checked caller identity, and attempted CDK bootstrap-role lookup.                                                                                                | Expected the authenticated principal to assume bootstrap roles; actual login resolved to account root, which cannot assume those roles.                                                                        | Medium   | Reviewed the exact account/region and template diff, then performed the authorized deployment directly with the authenticated principal.                                                                      | Recommend or provision a least-privilege deployment principal immediately after bootstrap, with a documented root exit.     |
| 2026-09-02 | Bedrock model access       | Listed Nova models/profiles, checked entitlement/regions, disabled invocation logging, and ran a minimal Converse call.                                                                           | Expected the active US Nova Micro profile to invoke; actual authorization was `NOT_AUTHORIZED` and Converse returned `Operation not allowed`.                                                                  | High     | Left `BedrockModelId` blank, omitted conditional model IAM, and kept every safety workflow deterministic and available.                                                                                       | Provide one regional preflight command that resolves the profile and reports authorization before application deployment.   |
| 2026-09-02 | CDK parameter review       | Passed CloudFormation parameter values to the pinned CLI during a pre-deploy diff.                                                                                                                | Expected parameter values to appear in the reviewed diff; actual `cdk diff` ignored them while `deploy` accepted them.                                                                                         | Medium   | Removed parameters from diff commands, documented separate value review, and made deploy parameters stack-qualified.                                                                                          | Make unsupported diff parameters fail closed or clearly report that their values are not included in the comparison.        |
| 2026-09-02 | SES sandbox                | Verified the identity, checked account sending state, sent a minimal application email, submitted a truthful transactional production-access request, and reviewed AWS's follow-up on 2026-09-03. | Expected the initial detail to be sufficient; actual delivery remained limited to verified identities and AWS requested frequency, list-management, bounce/complaint, unsubscribe, and sample-content details. | Medium   | Kept the single-recipient demo limitation explicit and prepared an evidence-backed reply covering the verified domain, suppression, idempotency, SQS/DLQ alarms, operator removal, and exact message samples. | Ask these process questions in the production-access form itself and expose the review state through the SES API.           |
| 2026-09-02 | CloudWatch Logs KMS policy | Deployed encrypted Lambda and API Gateway log groups with the customer-managed key.                                                                                                               | Expected CDK grants to cover encrypted log creation; actual CloudFormation rejected the groups without the regional Logs service principal.                                                                    | High     | Added a key-policy statement limited by the Logs encryption-context ARN prefixes and added a synth assertion before redeployment.                                                                             | Provide an L2 construct or validation that emits the required regional Logs principal policy for encrypted log groups.      |
| 2026-09-03 | CloudFront account gate    | Reviewed the preferred private-S3 distribution, added uncached API behaviors for `/v1/*`, `/mcp`, and `/.well-known/*`, and retried deployment.                                                   | Expected the corrected distribution to provision; AWS again returned the account-verification 403 and CloudFormation rolled back cleanly.                                                                      | High     | Kept the live API Gateway/private-S3 reader, verified every public route after rollback, and retained the contract-preserving CloudFront topology behind its gate.                                            | Expose account verification status and remediation steps before distribution creation or CDK deployment.                    |
| 2026-09-02 | EventBridge to SQS KMS     | Published a live domain event through EventBridge to AWS-managed-KMS-encrypted SQS targets.                                                                                                       | Expected queue delivery; actual EventBridge could not use `alias/aws/sqs`, so the notification target recorded a failed invocation.                                                                            | High     | Changed application queues and DLQs to SQS-managed server-side encryption, redeployed, and proved email/metric delivery with zero backlog.                                                                    | Document the EventBridge incompatibility prominently and flag it during CDK synth when using the AWS-managed SQS key.       |
| 2026-09-02 | Fallback cold starts       | Ran four parallel deployed Playwright sessions against the API Gateway/static-Lambda fallback.                                                                                                    | Expected the local 30-second navigation budget to hold; actual simultaneous cold starts exceeded it although serial requests succeeded.                                                                        | Medium   | Kept fast four-viewport checks local and ran deployed judge-path checks serially with a 90-second cold-start allowance.                                                                                       | Add a provider-supported lightweight private-S3 static origin for accounts temporarily blocked from CloudFront.             |
| 2026-09-02 | pnpm supply-chain policy   | Installed the pinned workspace with build-script approval enforced and classified every requested native script.                                                                                  | Expected only runtime-required builds; actual agent transitive packages requested unrelated local-LLM/tree-sitter native builds.                                                                               | Medium   | Denied unrelated builds and explicitly allowed only SWC, Sharp, CDK, watcher, and esbuild scripts in the committed policy.                                                                                    | Publish dependency-purpose metadata so package managers can distinguish optional native tooling from required runtime code. |
| 2026-09-02 | MCP SDK v2                 | Initialized the legacy Streamable HTTP path with a JSON-oriented protocol test.                                                                                                                   | Expected JSON under the configured response mode; actual the legacy initialize response used a valid SSE envelope.                                                                                             | Medium   | Updated the contract test to accept both valid JSON and SSE envelopes while retaining strict protocol/content validation.                                                                                     | Document clearly which initialization eras honor JSON response mode and which always use SSE.                               |
| 2026-09-02 | CDK / cdk-nag v3           | Tried to acknowledge granular IAM findings through the public validation API using their exact IDs.                                                                                               | Expected exact finding IDs to pass through; actual IDs contain `::`, which the acknowledgment API reserves as a delimiter.                                                                                     | Low      | Stored exact finding IDs in CDK's acknowledged-rule metadata with specific evidence and left every rule enabled.                                                                                              | Accept structured finding identifiers or encode delimiters instead of overloading a flat string.                            |
| 2026-09-02 | Next.js browser readiness  | Loaded the exported PWA and immediately clicked a visible demo control in automated browser tests.                                                                                                | Expected visible controls to be interactive; actual the first click could land before client hydration and be dropped.                                                                                         | High     | Disabled interactive controls until browser-session initialization completed and exposed an explicit readiness attribute.                                                                                     | Provide a first-class hydration/readiness signal for static-export E2E and assistive-technology testing.                    |
| 2026-09-02 | Next.js generated types    | Ran development, formatting, and then clean-host CI against framework-owned `next-env.d.ts`.                                                                                                      | Expected stable generated declarations; actual `next dev` rewrote quote style and caused the clean format gate to fail.                                                                                        | Medium   | Excluded the framework-owned declaration from Prettier while still verifying generated OpenAPI/client contracts byte-for-byte.                                                                                | Keep framework-generated declaration output deterministic across dev and build modes.                                       |
| 2026-09-02 | pnpm workspace CI          | Installed Playwright from the repository root and launched the browser stage in GitHub Actions.                                                                                                   | Expected root `pnpm exec` to locate the binary; actual it could not see the executable owned by the `@stay/web` workspace.                                                                                     | Medium   | Scoped browser installation to `@stay/web` and updated actions to current Node-compatible majors; CI now passes 20/20 scenarios.                                                                              | Add clearer workspace-executable resolution diagnostics and examples to pnpm/Playwright CI guidance.                        |
