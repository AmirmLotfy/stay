# Product and developer friction log

| Date       | Surface                    | Observation                                                                                                                                          | Impact                                                                                                                  | Workaround / feedback                                                                                                                                                                                               |
| ---------- | -------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-09-02 | Alexa+ access              | MCP Toolkit access is limited to selected partners, so a general developer cannot assume add-on deployment or device tests.                          | Blocks guaranteed real-Alexa evidence.                                                                                  | Built one standards-based MCP server plus a polished web simulator, while keeping real deployment as a separately verified enhancement. A public access-request status endpoint would improve planning.             |
| 2026-09-02 | Alexa+ docs / MCP          | Current Alexa client examples reference a legacy 2025 initialization while the hackathon asks for MCP `2025-11-25` or later.                         | A server can accidentally satisfy one path but not the other.                                                           | Registered tools once and served both current and stateless legacy eras through the official SDK; added an explicit `2025-11-25` negotiation test. A compatibility matrix in the Alexa docs would reduce ambiguity. |
| 2026-09-02 | AWS authentication         | The approved remote login succeeded as the account root principal; root cannot assume the existing CDK bootstrap roles.                              | CDK can inspect through same-account credentials, but initial deployment will use the authenticated principal directly. | Recorded the exact account and region, reviewed the template-only diff, and kept all mutation behind the deployment parameter gate. A dedicated deployment principal remains the post-bootstrap operating path.     |
| 2026-09-02 | Bedrock model access       | Nova Micro, its US profile, entitlement, and regions are available, but the account reports `authorizationStatus: NOT_AUTHORIZED`.                   | The minimal Converse request returns `Operation not allowed`; AI cannot truthfully be claimed live.                     | Leave `BedrockModelId` blank, omit the conditional Bedrock IAM policy, and keep deterministic workflows operational. Recheck the same profile after AWS clears the account-level authorization.                     |
| 2026-09-02 | CDK parameter review       | The pinned CDK CLI accepts CloudFormation parameters on `deploy`, not `diff`.                                                                        | A copied `cdk diff --parameters` command silently ignored the values during review.                                     | Removed parameters from diff commands, documented separate value review, and made deploy parameters stack-qualified.                                                                                                |
| 2026-09-02 | SES sandbox                | SES sending is enabled and healthy, the approved identity is verified, and application delivery passed; production access is still disabled.         | Judges can see proven email behavior, but unverified recipients remain unavailable.                                     | Use the verified identity for sender/recipient in the demo and report sandbox status separately from successful delivery.                                                                                           |
| 2026-09-02 | CloudWatch Logs KMS policy | The first stack attempt referenced the customer key from encrypted log groups without granting the regional Logs service principal.                  | CloudFormation rejected the log groups and rolled the initial attempt back before serving traffic.                      | Added a key-policy statement limited by `kms:EncryptionContext:aws:logs:arn` to STAY Lambda and API Gateway log-group prefixes; retained encryption and added a synth assertion before redeployment.                |
| 2026-09-02 | CloudFront account gate    | AWS rejected new CloudFront resources until the account completes provider verification.                                                             | The preferred CDN topology could not be deployed, and a public S3 workaround would violate the security boundary.       | Deployed the same static build through API Gateway and a Lambda reader over the private KMS-encrypted bucket. Kept CloudFront as a separate reviewed upgrade after AWS clears the account gate.                     |
| 2026-09-02 | EventBridge to SQS KMS     | EventBridge could not deliver to queues encrypted with the AWS-managed `alias/aws/sqs` key because that key policy cannot grant the service.         | The first live domain event reached WebSocket but the notification target recorded a failed invocation.                 | Changed all application queues and DLQs to SQS-managed server-side encryption, reviewed the in-place diff, redeployed, and proved email plus metric delivery with zero DLQ backlog.                                 |
| 2026-09-02 | Fallback cold starts       | Four simultaneous browser sessions against the secure API Gateway/static-Lambda fallback exceeded the default 30-second Playwright navigation limit. | A parallel live test produced false negatives even though individual HTTPS requests and serial flows succeeded.         | Keep CI’s fast local four-viewport suite; run deployed judge-path checks serially with a 90-second cold-start allowance. CloudFront remains the performance upgrade after account verification.                     |
| 2026-09-02 | pnpm supply-chain policy   | Agent SDK transitives requested native build scripts unrelated to STAY’s Bedrock path.                                                               | Strict install failed until each script was classified.                                                                 | Denied local-LLM/tree-sitter native builds, explicitly allowed only SWC, Sharp, CDK, watcher, and esbuild scripts. This is now committed and auditable.                                                             |
| 2026-09-02 | MCP SDK v2                 | Legacy Streamable HTTP initialize responses use SSE even when the modern response mode is JSON.                                                      | A JSON-only protocol test produced a false failure.                                                                     | The test accepts both valid JSON and SSE envelopes. SDK docs could call out that the response-mode option applies only to the modern era more prominently.                                                          |
| 2026-09-02 | CDK / cdk-nag v3           | Granular IAM finding IDs contain `::`, while the current `Validations.acknowledge` API reserves the same delimiter.                                  | Documented acknowledgements cannot be passed through the public method unchanged.                                       | Recorded exact finding IDs through CDK's acknowledged-rule metadata key with specific evidence; no blanket rule was disabled. Upstream should encode finding IDs or accept a structured acknowledgment.             |
| 2026-09-02 | Next.js browser readiness  | A fast automated click could occur before the exported client component hydrated.                                                                    | The visible button appeared interactive but the first action could be dropped.                                          | Disabled interactive demo controls until the browser-local session effect completes and exposed a readiness attribute; the flow now passes across all four target viewports.                                        |
| 2026-09-02 | Next.js generated types    | `next dev` rewrites framework-owned `next-env.d.ts` after formatting, so its quote style changed before the first push.                              | The clean-host CI format gate failed before tests despite a green local run.                                            | Excluded the generated declaration from Prettier while continuing to format source and verify generated OpenAPI/client contracts byte-for-byte.                                                                     |
| 2026-09-02 | pnpm workspace CI          | Root `pnpm exec` could not discover Playwright because the executable belongs to the `@stay/web` workspace.                                          | GitHub CI completed all source, test, build, and synth gates but stopped before browser tests.                          | Scoped browser installation to `@stay/web` and updated GitHub actions to their current Node 24-compatible majors.                                                                                                   |
